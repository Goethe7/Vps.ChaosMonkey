using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Newtonsoft.Json;
using Vps.Chaos.Controllers;

namespace Vps.ChaosMonkey.Tests
{
    [TestClass]
    public class ChaosMonkeyTests
    {
        [TestMethod]
        public void TestChaosHeartBeat()
        {
            //var chaosController = new ChaosMonkeyController();
            //var heartBeat = JsonConvert.DeserializeObject<DateTime>(chaosController.HeartBeat()) ?? new DateTime());
            //Assert.IsNotNull(heartBeat);

        }
    }
}